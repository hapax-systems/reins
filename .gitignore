# Go build artifacts
/reins
/bin/
*.test
*.out

# per-machine Makefile overrides (instance, not engine)
reins.local.mk

# Python
__pycache__/
*.pyc
/api/.venv/
/api/uv.lock

# instance config (never commit real paths/secrets)
/config.toml

# property-based testing artifact cache (regenerable)
/.hypothesis/
